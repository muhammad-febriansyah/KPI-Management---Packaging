<div class="grid gap-4 sm:grid-cols-2"><label class="grid gap-2 text-sm font-semibold"><span>Kode <span class="text-danger">*</span></span><input name="code" required maxlength="50" placeholder="Contoh: OPS" value="{{ old('code', $costCenter?->code) }}" class="h-11 rounded-lg border border-line px-3 font-normal">@error('code')<span class="text-xs text-red-600">{{ $message }}</span>@enderror</label><label class="grid gap-2 text-sm font-semibold"><span>Nama <span class="text-danger">*</span></span><input name="name" required maxlength="100" placeholder="Contoh: Biaya Operasional" value="{{ old('name', $costCenter?->name) }}" class="h-11 rounded-lg border border-line px-3 font-normal">@error('name')<span class="text-xs text-red-600">{{ $message }}</span>@enderror</label></div><label class="grid max-w-sm gap-2 text-sm font-semibold"><span>Status <span class="text-danger">*</span></span><select name="status" required class="h-11 rounded-lg border border-line bg-white px-3 font-normal"><option value="active" @selected(old('status', $costCenter?->status ?? 'active') === 'active')>Aktif</option><option value="inactive" @selected(old('status', $costCenter?->status) === 'inactive')>Nonaktif</option></select></label>
